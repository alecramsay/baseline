baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s MI > data/MI/MI_2020_water_only.csv

	There are 40 water-only precincts

(3) scripts/extract_data.py -s MI

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s MI

(4) scripts/extract_graph.py -s MI

	Ran through here 06/21/23.

-----

(5) scripts/baseline_state.py -s MI -i 100 -c -v > intermediate/MI/MI20C_log_100.txt

(6) scripts/compare_maps.py -s MI -i 100 -v

	* Copy any missing maps output to maps/MI/MI20C_missing.txt.
	* Import MI20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s MI

	Ran through here 06/23/23.
	
(7) Copy the lowest energy baseline map to the maps/MI directory as MI20C_baseline_100.csv.

	MI20C_I004K01N13 => MI20C_I000K01N13 <<< Updated 06/28/23
	
--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s MI
    - scripts/analyze_state_part2.py -s MI

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s MI

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------