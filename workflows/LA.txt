baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s LA > data/LA/LA_2020_water_only.csv

	Six water-only precincts
	
(3) scripts/extract_data.py -s LA

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s XX

(4) scripts/extract_graph.py -s LA

	Ran through here 06/21/23.

--------

(5) scripts/baseline_state.py -s LA -i 100 -c -v > intermediate/LA/LA20C_log_100.txt

(6) scripts/compare_maps.py -s LA -i 100 -v

	* Copy any missing maps output to maps/LA/LA20C_missing.txt.
	* Import LA20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s LA

(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/LA directory as LA20C_baseline_100.csv.

	LA20C_I004K01N06 => LA20C_I026K01N06 <<< Updated 06/27/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s XX
    - scripts/analyze_state_part2.py -s XX

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s XX

(6) Turn the state on in the website

    [ ] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------