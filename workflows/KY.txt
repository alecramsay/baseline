baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s KY > data/KY/KY_2020_water_only.csv

		Three water-only precincts
	
(3) scripts/extract_data.py -s KY

(4) scripts/extract_graph.py -s KY

		Ran through here 06/21/23.

(5) scripts/baseline_state.py -s KY -i 100 -c -v > intermediate/KY/KY20C_log_100.txt

		* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s KY -i 100 -v

		* Copy any missing maps output to maps/KY/KY20C_missing.txt.
		* Import KY20C_candidates.csv into a spreadheet, and verify that the results are OK.

		Update the energies:

		scripts/update_energies.py -s KY
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/KY directory as KY20C_baseline_100.csv.

		KY20C_I017K01N06 => KY20C_I015K01N06 <<< TODO

--------

pg:

(5) scripts/import_base_map.sh KY

		Ran through here 06/24/23.
		TODO: do from here down	

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to KY_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/KY/ folder

(7) scripts/pull_map_ratings.sh KY Congress Baseline xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

(8) Tweak each duplicated Official and Notable map in DRA
		* KY_2022_Congress_Compact_map.png
		* KY_2022_Congress_Competitive_map.png
		* KY_2022_Congress_Minority_map.png
		* KY_2022_Congress_Official_map.png
		* KY_2022_Congress_Proportional_map.png
		* KY_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(9) scripts/analyze_state.py KY

(10) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/KY/KY20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/KY.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------