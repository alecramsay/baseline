baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s AR > data/AR/AR_2020_water_only.csv

		No water-only VTDs (precincts)
	
(3) scripts/extract_data.py -s AR

(4) scripts/extract_graph.py -s AR

		Ran through here 06/21/23.

(5) scripts/baseline_state.py -s AR -i 100 -c -v > intermediate/AR/AR20C_log_100.txt

(6) scripts/compare_maps.py -s AR -i 100 -v

		* Copy any missing maps output to maps/AR/AR20C_missing.txt.
		* Import AR20C_candidates.csv into a spreadheet, and verify that the results are OK.

		Update the energies:

		scripts/update_energies.py -s AR
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/AR directory as AR20C_baseline_100.csv.

		AR20C_I097K01N04 => AR20C_I015K01N04 <<< Updated 06/26/23

--------

pg:

(5) scripts/import_base_map.sh AR

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to AR_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/AR/ folder

		Ran through here 06/27/23.

(7) Tweak each duplicated Official and Notable map in DRA
		* AR_2022_Congress_Compact_map.png
		* AR_2022_Congress_Competitive_map.png
		* AR_2022_Congress_Minority_map.png
		* AR_2022_Congress_Official_map.png
		* AR_2022_Congress_Proportional_map.png
		* AR_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(8) scripts/analyze_state.py AR

(9) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/AR/AR20C_log_100.txt | awk 'END{print}'
	
(10) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/AR.markdown, change the layout to 'state' and remove the NYI one-liner
	
		Ran through here 07/01/23.

--------