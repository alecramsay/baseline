baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s GA > data/GA/GA_2020_water_only.csv

		No water-only VTDs (precincts) for GA

(3) scripts/extract_data.py -s GA

(4) scripts/extract_graph.py -s GA

		Ran through here 06/21/23.
		
(5) scripts/baseline_state.py -s GA -i 100 -c -v > intermediate/GA/GA20C_log_100.txt

		* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s GA -i 100 -v

		* Copy any missing maps output to maps/GA/GA20C_missing.txt.
		* Import GA20C_candidates.csv into a spreadheet, and verify that the results are OK.

(7) Copy the lowest energy baseline map to the maps/GA directory as GA20C_baseline_100.csv.

		GA20C_I073K01N14 => GA20C_I051K01N14

--------

pg:

(5) scripts/import_base_map.sh GA

		Ran through here 06/22/23.
		TODO: do from here down

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to GA_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/GA/ folder

(7) scripts/pull_map_ratings.sh GA Congress Baseline fc0fd1f1-38f8-4513-aa26-f69e111ca3a1

(8) Tweak each duplicated Official and Notable map in DRA
		* GA_2022_Congress_Compact_map.png
		* GA_2022_Congress_Competitive_map.png
		* GA_2022_Congress_Minority_map.png
		* GA_2022_Congress_Official_map.png
		* GA_2022_Congress_Proportional_map.png
		* GA_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(9) scripts/analyze_state.py GA

(10) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/GA/GA20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/GA.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------