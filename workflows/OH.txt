baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s OH > data/OH/OH_2020_water_only.csv

	There are eight water-only precincts
	
(3) scripts/extract_data.py -s OH

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s OH

(4) scripts/extract_graph.py -s OH

	Ran through here 06/21/23.

-----

(5) scripts/baseline_state.py -s OH -i 100 -c -v > intermediate/OH/OH20C_log_100.txt

(6) scripts/compare_maps.py -s OH -i 100 -v

	* Copy any missing maps output to maps/OH/OH20C_missing.txt.
	* Import OH20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s OH

	Ran through here 06/24/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/OH directory as OH20C_baseline_100.csv.

	OH20C_I042K01N15 => OH20C_I078K01N15 <<< Updated 06/28/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s OH
    - scripts/analyze_state_part2.py -s OH

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s OH

(6) Turn the state on in the website

    [ ] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------