baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s OK > data/OK/OK_2020_water_only.csv

	There is one water-only precinct
	
(3) scripts/extract_data.py -s OK

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s OK

(4) scripts/extract_graph.py -s OK

	Ran through here 06/21/23.

--------
		
(5) scripts/baseline_state.py -s OK -i 100 -c -v > intermediate/OK/OK20C_log_100.txt

(6) scripts/compare_maps.py -s OK -i 100 -v

	* Copy any missing maps output to maps/OK/OK20C_missing.txt.
	* Import OK20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s OK

	Ran through here 06/24/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/OK directory as OK20C_baseline_100.csv.

		OK20C_I016K01N05 => OK20C_I096K01N05 <<< Updated 06/28/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s OK
    - scripts/analyze_state_part2.py -s OK

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s OK

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------