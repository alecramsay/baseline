baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s MS > data/MS/MS_2020_water_only.csv

	No water-only VTDs (precincts)
	
(3) scripts/extract_data.py -s MS

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s MS

(4) scripts/extract_graph.py -s MS

	Ran through here 06/21/23.

--------
		
(5) scripts/baseline_state.py -s MS -i 100 -c -v > intermediate/MS/MS20C_log_100.txt

(6) scripts/compare_maps.py -s MS -i 100 -v

	* Copy any missing maps output to maps/MS/MS20C_missing.txt.
	* Import MS20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s MS

	Ran through here 06/24/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/MS directory as MS20C_baseline_100.csv.

	MS20C_I067K01N04 => MS20C_I046K01N04 <<< Updated

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s MS
    - scripts/analyze_state_part2.py -s MS

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s MS

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------