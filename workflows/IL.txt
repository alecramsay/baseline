baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s IL > data/IL/IL_2020_water_only.csv

		There are two water-only precincts
	
(3) scripts/extract_data.py -s IL

(4) scripts/extract_graph.py -s IL

		Ran through here 06/21/23.

(5) scripts/baseline_state.py -s IL -i 100 -c -v > intermediate/IL/IL20C_log_100.txt

		* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s IL -i 100 -v

		* Copy any missing maps output to maps/IL/IL20C_missing.txt.
		* Import IL20C_candidates.csv into a spreadheet, and verify that the results are OK.

(7) Copy the lowest energy baseline map to the maps/IL directory as IL20C_baseline_100.csv.

		IL20C_I050K01N17
	
--------

pg:

(5) scripts/import_base_map.sh IL

		Ran through here 06/23/23.
		TODO: do from here down	

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to IL_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/IL/ folder

(7) scripts/pull_map_ratings.sh IL Congress Baseline ed830071-22bf-4a30-92ce-a4d2f9a7dd8a

(8) Tweak each duplicated Official and Notable map in DRA
		* IL_2022_Congress_Compact_map.png
		* IL_2022_Congress_Competitive_map.png
		* IL_2022_Congress_Minority_map.png
		* IL_2022_Congress_Official_map.png
		* IL_2022_Congress_Proportional_map.png
		* IL_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(9) scripts/analyze_state.py IL

(10) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/IL/IL20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website <- NOT DONE YET, BECAUSE THE BASELINE IS NOT CONTIGUOUS

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/IL.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------