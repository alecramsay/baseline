baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s CT > data/CT/CT_2020_water_only.csv

	No water-only VTDs (precincts)
	
(3) scripts/extract_data.py -s CT

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s CT

(4) scripts/extract_graph.py -s CT

	Ran through here 06/21/23.

-----
		
(5) scripts/baseline_state.py -s CT -i 100 -c -v > intermediate/CT/CT20C_log_100.txt

(6) scripts/compare_maps.py -s CT -i 100 -v

		* Copy any missing maps output to maps/CT/CT20C_missing.txt.
		* Import CT20C_candidates.csv into a spreadheet, and verify that the results are OK.

		Update the energies:

		scripts/update_energies.py -s CT
		
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/CT directory as CT20C_baseline_100.csv.

		CT20C_I075K01N05 <<< verified 06/26/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s CT
    - scripts/analyze_state_part2.py -s CT

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s CT

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------