baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s AZ > data/AZ/AZ_2020_water_only.csv

		There is one ZZZZZZ "water-only" precinct

(3) scripts/extract_data.py -s AZ

(4) scripts/extract_graph.py -s AZ

		Ran through here 06/21/23.

(5) scripts/baseline_state.py -s AZ -i 100 -c -v > intermediate/AZ/AZ20C_log_100.txt

		* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s AZ -i 100 -v

		* Copy any missing maps output to maps/AZ/AZ20C_missing.txt.
		* Import AZ20C_candidates.csv into a spreadheet, and verify that the results are OK.

(7) Copy the lowest energy baseline map to the maps/AZ directory as AZ20C_baseline_100.csv.

		AZ20C_I001K01N09

--------

pg:

(5) scripts/import_base_map.sh AZ

		Ran through here 06/24/23.
		TODO: do from here down

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to AZ_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/AZ/ folder
		
		Reassigned the one stray (unassigned) precinct.

(7) scripts/pull_map_ratings.sh AZ Congress Baseline 83e28203-c0c9-4034-bc99-9aa024945b3a

(8) Tweak each duplicated Official and Notable map in DRA
		* AZ_2022_Congress_Compact_map.png
		* AZ_2022_Congress_Competitive_map.png
		* AZ_2022_Congress_Minority_map.png
		* AZ_2022_Congress_Official_map.png
		* AZ_2022_Congress_Proportional_map.png
		* AZ_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(9) scripts/analyze_state.py AZ

(10) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/AZ/AZ20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/AZ.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------