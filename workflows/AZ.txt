baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s AZ > data/AZ/AZ_2020_water_only.csv

	There is one ZZZZZZ "water-only" precinct

(3) scripts/extract_data.py -s AZ

	Ran this separately 08/20/23

	scripts/extract_blocks_by_vtd.py -s AZ

(4) scripts/extract_graph.py -s AZ

	Ran through here 06/21/23.

--------

(5) scripts/baseline_state.py -s AZ -i 100 -c -v > intermediate/AZ/AZ20C_log_100.txt

(6) scripts/compare_maps.py -s AZ -i 100 -v

	* Copy any missing maps output to maps/AZ/AZ20C_missing.txt.
	* Import AZ20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s AZ

(7) Copy the lowest energy baseline map to the maps/AZ directory as AZ20C_baseline_100.csv.

	AZ20C_I001K01N09 <<< verified 06/26/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s AZ
    - scripts/analyze_state_part2.py -s AZ

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s AZ

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.
		
--------