baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s NH > data/NH/NH_2020_vtd_water_only.csv

(3) scripts/extract_data.py -s NH

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s NH

(4) scripts/extract_graph.py -s NH

-----

(5) scripts/baseline_state.py -s NH -i 100 -c -v > intermediate/NH/NH20C_log_100.txt

(6) scripts/compare_maps.py -s NH -i 100 -v

	* Copy any missing maps output to maps/NH/NH20C_missing.txt.
	* Import NH20C_candidates.csv into a spreadheet, and verify that the results are OK.

	If necessary, update the energies:

	scripts/update_energies.py -s NH
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/NH directory as NH20C_baseline_100.csv.

	NH20C_I006K01N02

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s NH
    - scripts/analyze_state_part2.py -s NH

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s NH

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------