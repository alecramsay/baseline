baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s TX > data/TX/TX_2020_water_only.csv

	There are 10 water-only precincts
	
(3) scripts/extract_data.py -s TX

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s TX

(4) scripts/extract_graph.py -s TX

	Ran through here 06/21/23.

(5) scripts/baseline_state.py -s TX -i 100 -c -v > intermediate/TX/TX20C_log_100.txt

(6) scripts/compare_maps.py -s TX -i 100 -v

	* Copy any missing maps output to maps/TX/TX20C_missing.txt.
	* Import TX20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s TX

	Ran through here 06/24/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/TX directory as TX20C_baseline_100.csv.

	TX20C_I057K01N38 <<< verified 06/26/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s TX
    - scripts/analyze_state_part2.py -s TX

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s TX

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------