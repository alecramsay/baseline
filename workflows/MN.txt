baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s MN > data/MN/MN_2020_water_only.csv

	Four water-only precincts
	
(3) scripts/extract_data.py -s MN -w
(4) scripts/extract_graph.py -s MN -w
(5) scripts/baseline_state.py -s MN -i 100 -c -v > intermediate/MN/MN20C_log_100.txt

	* Review "WARNING" messages in the log.

(6) scripts/compare_maps.py -s MN -i 100 -v

	* Copy any missing maps output to maps/MN/MN20C_missing.txt.
	* Import MN20C_candidates.csv into a spreadheet, and verify that the results are OK.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/MN directory as MN20C_baseline_100.csv.

	MN20C_I043K01N08 <= NOTE - Not the lowest energy map!

--------

pg:

(5) scripts/import_base_map.sh MN

	Verify the map in DRA.

(6) Open the map in DRA:
	* Set the Colors and Overlays -- 'Change Palette' to Plasma
	* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
	* Export the block-assignment file
	* Rename it to MN_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
	* Move it to the data/MN/ folder
(7) scripts/pull_map_ratings.sh MN Congress Baseline xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
(8) Tweak each duplicated Official and Notable map in DRA
	* MN_2022_Congress_Compact_map.png
	* MN_2022_Congress_Competitive_map.png
	* MN_2022_Congress_Minority_map.png
	* MN_2022_Congress_Official_map.png
	* MN_2022_Congress_Proportional_map.png
	* MN_2022_Congress_Splitting_map.png
	* Move them to the docs/assets/images/ folder
	* Add the maps to the Group
(9) scripts/analyze_state.py MN


(10) Add population deviation & runtime to the abstract spreadsheet

	cat intermediate/MN/MN20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

	* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
	* On the state's page in docs/_pages/pages/MN.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------