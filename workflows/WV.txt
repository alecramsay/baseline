NOTE
- Use BG's
--------
baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s WV > data/WV/WV_2020_water_only.csv

(3) scripts/extract_data.py -s WV

(4) scripts/extract_graph.py -s WV

(5) scripts/baseline_state.py -s WV -i 100 -c -v > intermediate/WV/WV20C_log_100.txt

(6) scripts/compare_maps.py -s WV -i 100 -v

		* Copy any missing maps output to maps/WV/WV20C_missing.txt.
		* Import WV20C_candidates.csv into a spreadheet, and verify that the results are OK.

		If necessary, update the energies:

		scripts/update_energies.py -s WV
		
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/WV directory as WV20C_baseline_100.csv.

		WV20C_I000K01N02

--------

pg:

(5) scripts/import_base_map.sh WV

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to WV_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/WV/ folder

		Ran through here 07/16/23

(7) Tweak each duplicated Official and Notable map in DRA
		* WV_2022_Congress_Compact_map.png
		* WV_2022_Congress_Competitive_map.png
		* WV_2022_Congress_Minority_map.png
		* WV_2022_Congress_Official_map.png
		* WV_2022_Congress_Proportional_map.png
		* WV_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(8) scripts/analyze_state.py WV

(9) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/WV/WV20C_log_100.txt | awk 'END{print}'
	
(10) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/WV.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------