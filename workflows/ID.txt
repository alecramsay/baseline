baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s ID > data/ID/ID_2020_vtd_water_only.csv

(3) scripts/extract_data.py -s ID

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s ID

(4) scripts/extract_graph.py -s ID

--------

(5) scripts/baseline_state.py -s ID -i 100 -c -v > intermediate/ID/ID20C_log_100.txt

(6) scripts/compare_maps.py -s ID -i 100 -v

	* Copy any missing maps output to maps/ID/ID20C_missing.txt.
	* Import ID20C_candidates.csv into a spreadheet, and verify that the results are OK.

	If necessary, update the energies:

	scripts/update_energies.py -s ID
		
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/ID directory as ID20C_baseline_100.csv.

	ID20C_I000K01N02 

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s ID
    - scripts/analyze_state_part2.py -s ID

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s ID

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------