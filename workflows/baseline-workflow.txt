-------------------
TO BASELINE A STATE
-------------------

baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s XX > data/XX/XX_2020_vtd_water_only.csv

	NOTE - Run this step but *don't* use the output (-w flag) in steps 3 & 4.

(3) scripts/extract_data.py -s XX

	Run this separately

	scripts/extract_blocks_by_vtd.py -s XX
    scripts/extract_blocks_by_bg.py -s XX   <= for CA, OR, and WV

(4) scripts/extract_graph.py -s XX

--------

(5) scripts/baseline_state.py -s XX -i 100 -c -v > intermediate/XX/XX20C_log_100.txt

	* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s XX -i 100 -v

	* Copy any missing maps output to maps/XX/XX20C_missing.txt.
	* Import XX20C_candidates.csv into a spreadheet, and verify that the results are OK.

	If necessary, update the energies:

	scripts/update_energies.py -s XX
		
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/XX directory as XX20C_baseline_100.csv.

    Add population deviation & runtimes to the abstract spreadsheet

    - cat intermediate/XX/XX20C_log_100.txt | awk 'END{print}'

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s XX
    - scripts/analyze_state_part2.py -s XX

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s XX

(6) Turn the state on in the website

    [ ] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.
	
--------