baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s MD > data/MD/MD_2020_water_only.csv

	Nine water-only precincts
	
(3) scripts/extract_data.py -s MD

	Ran this separately 08/20/23

	scripts/extract_blocks_by_vtd.py -s MD

(4) scripts/extract_graph.py -s MD

	Ran through here 06/21/23.

--------

(5) scripts/baseline_state.py -s MD -i 100 -c -v > intermediate/MD/MD20C_log_100.txt

(6) scripts/compare_maps.py -s MD -i 100 -v

	* Copy any missing maps output to maps/MD/MD20C_missing.txt.
	* Import MD20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s MD

	Ran through here 06/23/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/MD directory as MD20C_baseline_100.csv.

	MD20C_I093K01N08 => MD20C_I034K01N08 <<< Updated 06/28/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s MD <<< TODO - Fails
    - scripts/analyze_state_part2.py -s MD

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s MD

(6) Turn the state on in the website

    - Copy & paste the YAML fragment into the state.yml file. 
    - Change the state.md layout from 'page' to 'state.
    - Add qualitative analysis.

--------