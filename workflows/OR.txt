baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s OR > data/OR/OR_2020_water_only.csv

		There are 7 water-only BG's.
	
(3) scripts/extract_data.py -s OR

(4) scripts/extract_graph.py -s OR

		Ran through here 06/24/23.

(5) scripts/baseline_state.py -s OR -i 100 -c -v > intermediate/OR/OR20C_log_100.txt

		* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s OR -i 100 -v

		* Copy any missing maps output to maps/OR/OR20C_missing.txt.
		* Import OR20C_candidates.csv into a spreadheet, and verify that the results are OK.

		Update the energies:

		scripts/update_energies.py -s OR

		Ran through here 06/24/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/OR directory as OR20C_baseline_100.csv.

		OR20C_I051K01N06 => OR20C_I015K01N06 <<< Updated 06/28/23

--------

pg:

(5) scripts/import_base_map.sh OR

		Ran through here 06/24/23.
		TODO: do from here down	

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to OR_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/OR/ folder

(7) scripts/pull_map_ratings.sh OR Congress Baseline 84976420-5049-4e8e-bafa-985d4a392d97

(8) Tweak each duplicated Official and Notable map in DRA
		* OR_2022_Congress_Compact_map.png
		* OR_2022_Congress_Competitive_map.png
		* OR_2022_Congress_Minority_map.png
		* OR_2022_Congress_Official_map.png
		* OR_2022_Congress_Proportional_map.png
		* OR_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(9) scripts/analyze_state.py OR

(10) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/OR/OR20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/OR.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------