baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s IN > data/IN/IN_2020_water_only.csv

		Seven water-only precincts

(3) scripts/extract_data.py -s IN

(4) scripts/extract_graph.py -s IN

		Ran through here 06/21/23.
		
(5) scripts/baseline_state.py -s IN -i 100 -c -v > intermediate/IN/IN20C_log_100.txt

		* Review "WARNING" & "AUDIT" messages in the log.

(6) scripts/compare_maps.py -s IN -i 100 -v

		* Copy any missing maps output to maps/IN/IN20C_missing.txt.
		* Import IN20C_candidates.csv into a spreadheet, and verify that the results are OK.

		Update the energies:

		scripts/update_energies.py -s IN
		
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/IN directory as IN20C_baseline_100.csv.
	
		IN20C_I088K01N09 <<< verified 06/26/23

--------

pg:

(5) scripts/import_base_map.sh IN

		Ran through here 06/27/23.

(6) Open the map in DRA:
		* Set the Colors and Overlays -- 'Change Palette' to Plasma
		* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
		* Export the block-assignment file
		* Rename it to IN_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
		* Move it to the data/IN/ folder

		Ran through here 06/27/23.
		TODO: do from here down	

(7) scripts/pull_map_ratings.sh IN Congress Baseline 75a78afd-46b9-427f-9410-62204c3c5939

(8) Tweak each duplicated Official and Notable map in DRA
		* IN_2022_Congress_Compact_map.png
		* IN_2022_Congress_Competitive_map.png
		* IN_2022_Congress_Minority_map.png
		* IN_2022_Congress_Official_map.png
		* IN_2022_Congress_Proportional_map.png
		* IN_2022_Congress_Splitting_map.png
		* Move them to the docs/assets/images/ folder
		* Add the maps to the Group

(9) scripts/analyze_state.py IN

(10) Add population deviation & runtime to the abstract spreadsheet

		cat intermediate/IN/IN20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

		* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
		* On the state's page in docs/_pages/pages/IN.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------