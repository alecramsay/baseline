baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s CA > data/CA/CA_2020_bg_water_only.csv

	There are 23 water-only blockgroups.
	
(3) scripts/extract_data.py -s CA

	Ran this separately 08/22/23

    scripts/extract_blocks_by_bg.py -s CA 

(4) scripts/extract_graph.py -s CA -a

	Re-ran through here 06/27/23 using BG again.

--------

(5) scripts/baseline_state.py -s CA -i 100 -c -v > intermediate/CA/CA20C_log_100.txt

(6) scripts/compare_maps.py -s CA -i 100 -v

	* Copy any missing maps output to maps/CA/CA20C_missing.txt.
	* Import CA20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s CA
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/CA directory as CA20C_baseline_100_tracts.csv.

	CA20C_I064K01N52 => CA20C_I033K01N52 <<< Re-run 06/27/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s CA
    - scripts/analyze_state_part2.py -s CA

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s CA

(6) Turn the state on in the website

    [ ] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------