baseline:

(1) Create output directories
(2) scripts/extract_water_only.py -s VA > data/VA/VA_2020_water_only.csv

		There are no water-only VTDs (precincts)

(3) scripts/extract_data.py -s VA
(4) scripts/extract_graph.py -s VA

		Ran through here 06/21/23.
		
(5) scripts/baseline_state.py -s VA -i 100 -c -v > intermediate/VA/VA20C_log_100.txt

	* Review "WARNING" messages in the log.

(6) scripts/compare_maps.py -s VA -i 100 -v

	* Copy any missing maps output to maps/VA/VA20C_missing.txt.
	* Import VA20C_candidates.csv into a spreadheet, and verify that the results are OK.

(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/VA directory as VA20C_baseline_100.csv.

	VA20C_I051K01N11

--------

pg:

(5) scripts/import_base_map.sh VA

	Verify the map in DRA.

(6) Open the map in DRA:
	* Set the Colors and Overlays -- 'Change Palette' to Plasma
	* Copy the Share link guid to constants.py in both the 'baseline' and 'pg' repos
	* Export the block-assignment file
	* Rename it to VA_2020_Congress_Baseline.csv <<< NOTE - 2020 not 2022, and 'Baseline' not 'baseline'
	* Move it to the data/VA/ folder
(7) scripts/pull_map_ratings.sh VA Congress Baseline xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
(8) Tweak each duplicated Official and Notable map in DRA
	* VA_2022_Congress_Compact_map.png
	* VA_2022_Congress_Competitive_map.png
	* VA_2022_Congress_Minority_map.png
	* VA_2022_Congress_Official_map.png
	* VA_2022_Congress_Proportional_map.png
	* VA_2022_Congress_Splitting_map.png
	* Move them to the docs/assets/images/ folder
	* Add the maps to the Group
(9) scripts/analyze_state.py VA


(10) Add population deviation & runtime to the abstract spreadsheet

	cat intermediate/VA/VA20C_log_100.txt | awk 'END{print}'
	
(11) Turn the state on in the website

	* Add the Share link guid to states.yml and flip the 'ready' property for the state to 'true'
	* On the state's page in docs/_pages/pages/VA.markdown, change the layout to 'state' and remove the NYI one-liner
	
--------