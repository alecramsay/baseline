baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s NV > data/NV/NV_2020_vtd_water_only.csv

	There are two water-only precincts
	
(3) scripts/extract_data.py -s NV

	Ran this separately 08/21/23

	scripts/extract_blocks_by_vtd.py -s NV

(4) scripts/extract_graph.py -s NV

	Ran through here 06/21/23.

--------

(5) scripts/baseline_state.py -s NV -i 100 -c -v > intermediate/NV/NV20C_log_100.txt

(6) scripts/compare_maps.py -s NV -i 100 -v

	* Copy any missing maps output to maps/NV/NV20C_missing.txt.
	* Import NV20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s NV

	Ran through here 06/23/23.
	
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/NV directory as NV20C_baseline_100.csv.

	NV20C_I065K01N04 => NV20C_I098K01N04 <<< Updated 06/28/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s NV
    - scripts/analyze_state_part2.py -s NV

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s NV

(6) Turn the state on in the website

    [x] - Copy & paste the YAML fragment into the state.yml file. 
    [ ] - Change the state.md layout from 'page' to 'state.
    [ ] - Add qualitative analysis.

--------