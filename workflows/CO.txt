baseline:

(1) Create output directories

(2) scripts/extract_water_only.py -s CO > data/CO/CO_2020_water_only.csv

	No water-only VTDs (precincts)
	
(3) scripts/extract_data.py -s CO

	Ran this separately 08/20/23

	scripts/extract_blocks_by_vtd.py -s CO

(4) scripts/extract_graph.py -s CO

	Ran through here 06/21/23.

--------

(5) scripts/baseline_state.py -s CO -i 100 -c -v > intermediate/CO/CO20C_log_100.txt

(6) scripts/compare_maps.py -s CO -i 100 -v

	* Copy any missing maps output to maps/CO/CO20C_missing.txt.
	* Import CO20C_candidates.csv into a spreadheet, and verify that the results are OK.

	Update the energies:

	scripts/update_energies.py -s CO
		
(7) Copy the lowest energy contiguous candidate with population deviation <= 2% to the maps/CO directory as CO20C_baseline_100.csv.

	CO20C_I070K01N08 => CO20C_I044K01N08 <<< Updated 06/27/23

--------

pg:

(4) Analyze the official & notable maps relative to the baseline map

    - scripts/analyze_state_part1.py -s CO
    - scripts/analyze_state_part2.py -s CO

(5) Deploy the artifacts

    - scripts/DEPLOY.py -s CO

(6) Turn the state on in the website

    - Copy & paste the YAML fragment into the state.yml file. 
    - Change the state.md layout from 'page' to 'state.
    - Add qualitative analysis.

--------